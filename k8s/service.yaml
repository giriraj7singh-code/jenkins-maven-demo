apiVersion: v1
kind: Service

metadata:
  name: jenkins-maven-demo-service

spec:
  selector:
    app: jenkins-maven-demo

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: ClusterIP
